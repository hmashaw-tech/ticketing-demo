name: tests

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Step One
      uses: actions/checkout@v2
    - name: Step Two
      run: cd auth && npm install && npm run test:ci
      
